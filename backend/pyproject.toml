[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for splitting PDF files"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "pypdf>=5.0.0",
    "python-multipart>=0.0.20",
    "uvicorn[standard]>=0.30.0",
]
